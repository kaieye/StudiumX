# 本地数据待办

> 本文是本地数据**下一步工作的唯一入口**。已完成的架构决定及其证据见 [ADR 索引](adr/README.md)。
>
> 下列条目记录未完成范围及待批准的设计门；设计文档不是实现授权。个别条目会标明已实施的受限切片，但不得把该切片的建议、验证矩阵或候选 contract 扩大为 complete durable closure。

## 先决规则

1. 任何后续切片先在对应 design gate 获得 scope / owner / API 批准，再单独立项；不得直接修改其 writer 以“顺手迁移”。
2. canonical JSON、Markdown、JSONL、immutable record 和 Memory 文件仍是事实来源；projection、分区、sealing、summary、`.bak` 与 receipt 都不授权删除或替代事实来源。
3. [ADR-0004](adr/0004-shared-durable-publish-and-partial-consumer-migration.md) 记录的是**部分** consumer migration：C-4P8 已在受控 `write_workspace_file` 的文本文件 create / restricted-overwrite scope 关闭，但这不表示所有 writer 已迁移、完整 C-4P6、完整 C-4P9 或跨文件事务已经完成；C-4P6-S1 也不得扩大为完整 C-4P6。不得把 [ADR-0005](adr/0005-main-owned-trace-correlation-and-safe-logs.md) 解释为全局 actionId / retry / receipt。

## C-4：仍未完成的其它 durable writer / closure 设计门

### P8：Windows workspace publish：direct-path profile 已实施；strict durable profile 仍未实施

- C-4P8 的受控 text create / restricted-overwrite scope 保持关闭。2026-07-19 在用户明确批准不同 S3 contract 后，Windows 已注册一个参考 `codex-rust` 的 root-constrained direct-path profile：上层相对路径/root/realpath 检查，S2 `wx` no-clobber create，S3 对既有 single-link regular target 使用 non-creating `r+` truncate/write/sync，并在写后 exact reread；既有 approvalMode、operation journal replay 与隐私化 stable error 保持有效。
- 该 profile 的限制是实现事实：它不是 descriptor/HANDLE-relative traversal，不比较 target file ID，不是 CAS 或 POSIX atomic exchange，不保证 directory-fsync durability，也不能证明 external reparse/leaf replacement race 安全。必须在 UI、tool contract、测试和文档中按“Windows direct-path non-CAS”表述，不能声称为 Windows native durable publish 或 strict containment。
- **仍未完成：**2026-07-19 的 Windows host-native/SDK audit 已确认 `NtCreateFile` 的 `RootDirectory` + `OBJ_DONT_REPARSE` 可作为 HANDLE-relative S1/S2 的候选基础，但已审计的 `FileRenameInfo[/Ex]` / `ReplaceFileW` 等 API 不提供 S3 所需的 expected-target-file-ID compare-and-swap 或 exchange；仅在 publish 前检查 file ID 仍有 inspect-to-publish race。若将来要求 POSIX-equivalent Windows strict profile，仍必须先获得可审计的 Windows/NTFS S3 identity-precondition primitive，再实施 HANDLE-relative、reparse-point/junction-safe traversal、atomic no-overwrite / exchange、file/directory durability 和 adversarial host-native CI；不得把当前 direct-path profile 当作该 gate 的完成。

### P9：session-audit durable append

- 设计文档：[C-4P9 Session-audit durable append](plans/local-data-session-audit-durable-append-design.md)
- **已实施范围仍仅限 S2 + tests-only evidence：**`4b30220` / `5f47382` 完成 **P9-S2 audit 专用 framed、legacy-compatible、fixed-file durable append**；`c286a42`（`test(data): cover audit durable append recovery`）完成严格 **P9-S3 tests-only evidence slice**；`ab723a6`（`test(data): cover audit pre-write short-circuit`）完成严格 **P9-S4 tests-only evidence slice**；`47393f9`（`test(data): cover audit directory capability symmetry`）完成严格 **P9-S5 tests-only evidence slice**；`5f931c9`（`test(data): cover audit ledger failure recovery`）完成严格 **P9-S6 tests-only evidence slice**（ledger-own failure residual）；`816e403`（`test(data): cover concurrent identical audit saves`）完成严格 **P9-S7 tests-only evidence slice**（concurrent identical same-save）；`bee173f`（`test(data): cover audit divergent-trace conflict`）完成严格 **P9-S8 tests-only evidence slice**（trace conflict 不得误作 exact dedupe）；`dcb9bae`（`test(data): cover concurrent same-ID body conflict`）完成严格 **P9-S9 tests-only evidence slice**（concurrent same-ID body conflict）；`9d54c5e`（`test(data): cover markdown publish short-circuit residual`）完成严格 **P9-S10 tests-only evidence slice**（Markdown durable publish short-circuit residual）；`bab5d1e`（`test(data): cover markdown directory close residual`）完成严格 **P9-S11 tests-only evidence slice**（Markdown-phase directory close residual）；`2aec1bc`（`test(data): cover markdown directory sync residual`）完成严格 **P9-S12 tests-only evidence slice**（Markdown-phase directory fsync residual）；`5e35703`（`test(data): cover markdown directory open residual`）完成严格 **P9-S13 tests-only evidence slice**（Markdown-phase directory open residual）；`be460a4`（`test(data): cover json directory open residual`）完成严格 **P9-S14 tests-only evidence slice**（JSON-phase directory open residual）；`8779879`（`test(data): cover audit file open residual`）完成严格 **P9-S15 tests-only evidence slice**（audit file open fail-closed residual）；`07ecb54`（`test(data): cover audit file sync residual`）完成严格 **P9-S16 tests-only evidence slice**（audit file sync fail-closed residual）；`8848af7`（`test(data): cover audit file close residual`）完成严格 **P9-S17 tests-only evidence slice**（audit file close fail-closed residual）；`54cec58`（`test(data): cover audit file lstat residual`）完成严格 **P9-S18 tests-only evidence slice**（audit file lstat fail-closed residual）；`529febd`（`test(data): cover audit file stat residual`）完成严格 **P9-S19 tests-only evidence slice**（audit file stat fail-closed residual）；`8091193`（`test(data): cover audit incomplete write residual`）完成严格 **P9-S20 tests-only evidence slice**（audit incomplete write/read transfer fail-closed residual）；`9309b81`（`test(data): cover audit file EACCES residual`）完成严格 **P9-S21 tests-only evidence slice**（audit file open/sync/close EACCES fail-closed residual）；`79e9d8d`（`test(data): cover audit directory mkdir residual`）完成严格 **P9-S22 tests-only evidence slice**（audit directory mkdir fail-closed residual）；`c3f9be5`（`test(data): cover audit directory open fatal residual`）完成严格 **P9-S23 tests-only evidence slice**（audit/parent directory open fatal fail-closed residual）。`8a27fc9`（`test(data): cover audit directory sync fatal residual`）完成严格 **P9-S24 tests-only evidence slice**（audit/parent directory sync fatal residual）。`fc765d2`（`test(data): cover audit post-open non-file residual`）完成严格 **P9-S25 tests-only evidence slice**（post-open audit target non-file fail-closed residual）。`c3c8db5`（`test(data): cover audit directory mkdir fatal residual matrix`）完成严格 **P9-S26 tests-only evidence slice**（audit directory mkdir fatal residual matrix）。`4e3ce10`（`test(data): cover audit file write fatal residual matrix`）完成严格 **P9-S27 tests-only evidence slice**（audit file write fatal residual matrix）。`46a46ad`（`test(data): cover audit file write unknown error residual`）完成严格 **P9-S28 tests-only evidence slice**（audit file write unknown error residual）。`abe159d`（`test(data): cover audit file open EPERM/ENOSPC residual`）完成严格 **P9-S29 tests-only evidence slice**（audit file open EPERM/ENOSPC residual）。`905ffb9`（`test(data): cover audit file sync EPERM/ENOSPC residual`）完成严格 **P9-S30 tests-only evidence slice**（audit file sync EPERM/ENOSPC residual）。证据与实际验证入口见 [ADR-0004](adr/0004-shared-durable-publish-and-partial-consumer-migration.md)。
- **S2 实现范围：**仅固定 `.agent-sessions/<conversation-id>.jsonl`；不 rotation、不调用 generic `durable-jsonl`；per absolute audit path queue 覆盖 same-descriptor exact-byte read / validate / dedupe / framed append / file fsync+close，随后 audit directory、再 conversation parent directory durability confirmation。directory open/sync 仅五个 allowlist code 可降级为通用 warning；post-directory failure retry 会先 dedupe exact rows，之后才继续既有 ledger flow。
- **S3 历史 evidence：**`c286a42` 仅补齐 P9-S2 的 partial-write 与 archive-level failure/retry 定向证据：fixed-file non-rotating audit append 的真实 partial prefix、torn-tail framing、dedupe recovery，以及 archive-level audit file `sync`/`close`、audit directory `open`/`sync`/`close`、conversation parent directory `open`/`sync`/`close` failure 后的 clean retry；无生产语义改动。其定向 unit 覆盖 2 个文件、61 tests passed；当时实际运行 `pnpm run typecheck`、`pnpm run check:security`、`git diff --check` 均通过。
- **S4 新增 evidence 范围：**`ab723a6` 仅补齐 archive save 层的**首个 audit write 注入 `EIO` 且 audit 为 0 bytes**的 short-circuit/retry evidence：JSON/Markdown 保留、ledger 未执行；clean retry 后每个 canonical audit row 恰一条、ledger 恰一条。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-archive-durable.unit.test.ts`（1 file、27 tests passed）。
- **S5 新增 evidence 范围：**`47393f9` 仅修改测试，未修改 production code；Sol review approved。它对 audit directory 与 conversation parent directory 的 `open`/`sync` 做 capability symmetry 定向证据：五个 allowlist code `EINVAL`/`ENOSYS`/`ENOTSUP`/`EOPNOTSUPP`/`EISDIR` 各覆盖两层、两种操作，共 20 cases；每个成功且恰好产生一条固定通用 warning，warning 不泄露路径、内容、conversation/header/entry ID 或 trace；parent-directory `close` 返回 `EINVAL` 仍 fatal。验证入口与结果为：单独运行 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（当前 S5 本切片：1 file、51 tests passed）；与 archive durable 一起运行（当前 S5 本切片：2 files、78 tests passed）；另有 `pnpm run typecheck`、`pnpm run check:security`、`git diff --check` 通过。无 production/API/schema/order 变化。
- **S6 新增 evidence 范围：**`5f931c9` 仅修改 `tests/unit/agent-conversation-archive-durable.unit.test.ts`，加强既有 ledger-own failure residual：JSON/Markdown/audit 成功后 ledger 目标为 directory 导致 ledger append 失败；audit 保留期望 header 与 canonical entry IDs、不 rollback；retry 保持 exact audit bytes、不写 audit、恰一条 ledger 行（`entryId` + conversation id）；随后 idempotent save 保持 audit 与 ledger bytes 不变。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-archive-durable.unit.test.ts`（1 file、27 tests passed）。

- **S7 新增 evidence 范围：**`816e403` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 concurrent identical same-save residual：两路并发 append 同一 record 时 per-path queue 线性化，第二 open 在第一 descriptor lifecycle 结束前不启动；两者成功 resolve；一个 session header、entry IDs 唯一且 parent chain 正确；exact audit bytes 与单次顺序 write 一致。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、52 tests passed）。
- **S8 新增 evidence 范围：**`bee173f` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 trace/legacy residual 中 *trace conflict 不得误作 exact dedupe success*：on-disk 同 identity 但 trace 分叉的两行，retry 同 record 必须 throw `Conversation session audit contains divergent duplicate records.`，poisoned bytes 不变，无额外 `write:`、无 rewrite。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、53 tests passed）。
- **S9 新增 evidence 范围：**`dcb9bae` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 concurrency residual 中 concurrent same-ID 但 canonical body 分叉：per-path queue 线性化下一路成功、一路 reject `conflicts with its canonical record`，winner bytes 与单次顺序 write 一致，一个 session header、conflicted entry ID 唯一。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、54 tests passed）。
- **S10 新增 evidence 范围：**`9d54c5e` 仅修改 `tests/unit/agent-conversation-archive-durable.unit.test.ts`，将既有 Markdown write-failure short-circuit 并入 4-case matrix，并补齐 Markdown durable file sync / file close / rename 失败 residual：JSON 保留、Markdown 目标仍 `ENOENT`、不 append audit/ledger、无 temporary leftover。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-archive-durable.unit.test.ts`（1 file、30 tests passed）。
- **S11 新增 evidence 范围：**`bab5d1e` 仅修改 `tests/unit/agent-conversation-archive-durable.unit.test.ts`，补齐 Markdown-phase directory close residual：第二次 `close:conversation/...`（Markdown rename 之后）返回 `EIO` 时 JSON 与 Markdown 均已发布、不 append audit/ledger、无 temporary leftover、save 不报告成功。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-archive-durable.unit.test.ts`（1 file、31 tests passed）。
- **S12 新增 evidence 范围：**`2aec1bc` 仅修改 `tests/unit/agent-conversation-archive-durable.unit.test.ts`，补齐 Markdown-phase directory fsync residual：第二次 `sync:conversation/...`（Markdown rename 之后）返回 `EIO` 时 JSON 与 Markdown 均已发布、不 append audit/ledger、无 temporary leftover、save 不报告成功。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-archive-durable.unit.test.ts`（1 file、32 tests passed）。
- **S13 新增 evidence 范围：**`5e35703` 仅修改 `tests/unit/agent-conversation-archive-durable.unit.test.ts`，补齐 Markdown-phase directory open residual：第二次 `open:r:conversation/...`（Markdown rename 之后）返回 `EIO` 时 JSON 与 Markdown 均已发布、不 append audit/ledger、无 temporary leftover、save 不报告成功。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-archive-durable.unit.test.ts`（1 file、33 tests passed）。
- **S14 新增 evidence 范围：**`be460a4` 仅修改 `tests/unit/agent-conversation-archive-durable.unit.test.ts`，补齐 JSON-phase directory open residual：第一次 `open:r:conversation/...`（JSON rename 之后）返回 `EIO` 时 JSON 已发布、Markdown 仍缺失、不 append audit/ledger、无 temporary leftover、save 不报告成功。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-archive-durable.unit.test.ts`（1 file、34 tests passed）。


- **S15 新增 evidence 范围：**`8779879` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit file open fail-closed residual：对 audit 目标文件 open 注入 `EIO` 以及 directory-fsync allowlist 五码（`EINVAL`/`ENOSYS`/`ENOTSUP`/`EOPNOTSUPP`/`EISDIR`）均保持 fatal、不 capability downgrade、无 write、无 warning、无 audit 文件创建。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、60 tests passed）。
- **S16 新增 evidence 范围：**`07ecb54` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit file sync fail-closed residual：对 audit 目标文件 sync 注入 `EIO`/`EINVAL`/`ENOSYS`/`ENOTSUP`/`EOPNOTSUPP`/`EISDIR` 均保持 fatal、不 capability downgrade；`sync:${path}` 出现；不启动 directory open；无 warning。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、66 tests passed）。
- **S17 新增 evidence 范围：**`8848af7` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit file close fail-closed residual：对 audit 目标文件 close 注入 `EIO`/`EINVAL`/`ENOSYS`/`ENOTSUP`/`EOPNOTSUPP`/`EISDIR` 均保持 fatal、不 capability downgrade；`close:${path}` 出现；不启动 directory open；无 warning。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、72 tests passed）。
- **S18 新增 evidence 范围：**`54cec58` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit file lstat fail-closed residual：对 audit 目标 lstat 注入 `EIO`/`EINVAL`/`ENOSYS`/`ENOTSUP`/`EOPNOTSUPP`/`EISDIR`/`EACCES` 均保持 fatal、不 capability downgrade；`lstat:${path}` 出现；不 open、不启动 directory open；无 warning；audit 文件未创建。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、79 tests passed）。
- **S19 新增 evidence 范围：**`529febd` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit file stat fail-closed residual：对已 open 的 audit 目标 `stat` 注入 `EIO`/`EINVAL`/`ENOSYS`/`ENOTSUP`/`EOPNOTSUPP`/`EISDIR`/`EACCES` 均保持 fatal、不 capability downgrade；`stat:${path}` 出现；open 已发生但无 write、不启动 directory open；无 warning。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、86 tests passed）。
- **S20 新增 evidence 范围：**`8091193` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`（含 test-only `readPlan` 扩展），补齐 audit incomplete write/read transfer fail-closed residual：write/read 返回 `bytesWritten`/`bytesRead` 为 `0` 或 `NaN` 时分别 throw `could not be written completely` / `could not be read exactly`；不 capability downgrade；不启动 directory open；无 warning。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、90 tests passed）。
- **S21 新增 evidence 范围：**`9309b81` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，将 audit file open/sync/close fail-closed matrices 扩展包含 `EACCES`：均 fatal、不 capability downgrade；open 失败不创建文件、不启动 directory open；无 warning。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、93 tests passed）。
- **S22 新增 evidence 范围：**`79e9d8d` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`（含 test-only `mkdir` 观测 instrumentation），补齐 audit directory mkdir fail-closed residual：对 audit directory `mkdir` 注入 `EIO`/`EACCES` 均 fatal、不 lstat、不 open、不 capability downgrade、无 warning、无 audit 文件创建。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、95 tests passed）。
- **S23 新增 evidence 范围：**`c3f9be5` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit directory 与 conversation parent directory open fatal fail-closed residual：对 `open:r` 注入 `EACCES`/`EPERM`/`EIO`/unknown 均 fatal、不 capability downgrade、不继续 directory sync、无 warning；file append 已完成但 directory durability 失败仍 reject。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、103 tests passed）。
- **S24 新增 evidence 范围：**`8a27fc9` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit directory 与 conversation parent directory sync fatal fail-closed residual：对 directory `sync` 注入 `EACCES`/`EPERM`/`EIO`/unknown 在 audit-directory 与 parent-directory 均 fatal、不 capability downgrade、无 warning；file append 已完成但 directory durability 失败仍 reject。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、107 tests passed）。
- **S25 新增 evidence 范围：**`fc765d2` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 post-open audit target `stat` 报告 non-file 的 fail-closed residual：reject `not a regular file`、不 capability downgrade、不 read/write、不启动 directory durability、无 warning；test-only `statPlan` instrumentation。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、108 tests passed）。
- **S26 新增 evidence 范围：**`c3c8db5` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit directory mkdir fatal residual matrix：对 `mkdir` 注入 `EIO`/`EACCES`/`EPERM`/`ENOSPC`/`EINVAL`/unknown 均 fatal、不 capability downgrade、不 lstat/open/write/sync、无 warning、无 audit 文件创建。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、112 tests passed）。
- **S27 新增 evidence 范围：**`4e3ce10` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit file write fatal residual matrix：对首个 audit file `write` 注入 `EIO`/`EINVAL`/`ENOSYS`/`ENOTSUP`/`EOPNOTSUPP`/`EISDIR`/`EACCES`/`EPERM`/`ENOSPC` 均 fatal、不 capability downgrade、不启动 directory open/sync、无 warning。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、121 tests passed）。
- **S28 新增 evidence 范围：**`46a46ad` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，补齐 audit file write unknown error residual：首个 audit file `write` 注入 non-errno unknown Error 时 fatal、不 capability downgrade、不启动 directory open/sync、无 warning。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、122 tests passed）。
- **S29 新增 evidence 范围：**`abe159d` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，将 audit file open fatal residual matrix 扩到 `EPERM`/`ENOSPC`：open 失败 fatal、不 capability downgrade、不 write、不启动 directory open、无 warning、无 audit 文件创建。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、124 tests passed）。
- **S30 新增 evidence 范围：**`905ffb9` 仅修改 `tests/unit/agent-conversation-session-audit.unit.test.ts`，将 audit file sync fatal residual matrix 扩到 `EPERM`/`ENOSPC`：sync 失败 fatal、不 capability downgrade、不启动 directory open、无 warning。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/agent-conversation-session-audit.unit.test.ts`（1 file、126 tests passed）。
- **仍未完成：**C-4P9 整个 gate 未关闭。S2 的实现与 S3/S4/S5/S6/S7/S8/S9/S10/S11/S12/S13/S14/S15/S16/S17/S18/S19/S20/S21/S22/S23/S24/S25/S26/S27/S28/S29/S30 evidence 不表示 generic JSONL migration、跨文件 transaction、ledger authority 或 archive save-order 变更、repair、rotation、IPC/UI 已实施；S5 只补 capability symmetry 的这 20 个定向 tests-only cases，不是完整 capability matrix；S6 只补 ledger-own failure residual 的定向 tests-only cases；S7 只补 concurrent identical same-save 的定向 tests-only cases；S8 只补 divergent-trace conflict fail-closed 的定向 tests-only cases；S9 只补 concurrent same-ID body conflict fail-closed 的定向 tests-only cases；S10 只补 Markdown durable publish short-circuit residual 的定向 tests-only cases；S11 只补 Markdown-phase directory close residual 的定向 tests-only cases；S12 只补 Markdown-phase directory fsync residual 的定向 tests-only cases；S13 只补 Markdown-phase directory open residual 的定向 tests-only cases；S14 只补 JSON-phase directory open residual 的定向 tests-only cases；S15 只补 audit file open fail-closed residual 的定向 tests-only cases；S16 只补 audit file sync fail-closed residual 的定向 tests-only cases；S17 只补 audit file close fail-closed residual 的定向 tests-only cases；S18 只补 audit file lstat fail-closed residual 的定向 tests-only cases；S19 只补 audit file stat fail-closed residual 的定向 tests-only cases；S20 只补 audit incomplete write/read transfer fail-closed residual 的定向 tests-only cases；S21 只补 audit file open/sync/close `EACCES` fail-closed residual 的定向 tests-only cases；S22 只补 audit directory mkdir fail-closed residual 的定向 tests-only cases；S23 只补 audit/parent directory open fatal fail-closed residual 的定向 tests-only cases；S24 只补 audit/parent directory sync fatal fail-closed residual 的定向 tests-only cases；S25 只补 post-open audit target non-file fail-closed residual 的定向 tests-only cases；S26 只补 audit directory mkdir fatal residual matrix 的定向 tests-only cases；S27 只补 audit file write fatal residual matrix 的定向 tests-only cases；S28 只补 audit file write unknown error residual 的定向 tests-only cases；S29 只补 audit file open EPERM/ENOSPC residual 的定向 tests-only cases；S30 只补 audit file sync EPERM/ENOSPC residual 的定向 tests-only cases，不是完整 residual matrix 或生产功能。更广 file-level capability / generic JSONL、rotation、事务、ledger authority/save order 改造、repair、IPC/UI 及其它 residual matrix 仍保留在 design gate 中，继续等待后续批准。
- 禁止越界：继续 **non-rotating**；不得接入 `appendDurableJsonlLine()` 的默认 month / size rotation，也不得将 C-4P1 archive publish 或 C-5E trace 计为完整 P9。不得以 S2 改变 ledger authority、JSON → Markdown → audit → 既有 ledger queue → final verify 的顺序，或扩大为其它 JSONL writer。

### P6：learning-outcome durable settlement 的剩余 close-out 设计门

- 设计文档：[C-4P6 Learning outcome durable settlement](plans/local-data-learning-outcome-durable-settlement-design.md)；已实施范围和提交证据见 [ADR-0004](adr/0004-shared-durable-publish-and-partial-consumer-migration.md)。
- **已实施且仅限 S1 + S2 + S3 + S4 + S5 + S6 + S7 + S8 + S9 + S10 + S11 + S12 + S13 + S14 + S15 + S16 + S17 + S18 + S19 + S20 + S21 + S22 + S23 + S24 + S25 + S26 + S27 + S28 + S29 tests-only evidence：**`7292bf4` / `e02a086` 实现严格有序 publish、内置 ledger 的私有 writer-lock 覆盖、fail-closed injected load-only ledger，以及 authority-first controlled reconcile。`9847842`（`test(data): cover outcome publish crash recovery`）仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐单一 `after_outcome_publish` crash window 的恢复证据；没有 production/API/schema/path/order 变化。`1334513`（`test(data): cover outcome marker recovery`）只扩展同一个既有 unit `it`，补齐 settlement-marker durable rename 返回 `EIO` 后的受限 restart/reconcile evidence；没有 production/API/schema/path/order 变化，也不是新增 test count。`e821c69`（`test(data): cover settled outcome recovery`）新增独立 `it`，只覆盖已有 `after_settlement_marker` 的一次中断：marker 的 canonical rename 在当前平台 capability policy 规定的 durable primitive 完成后可见，且未到达 `before_catalog_reconcile`；restart `reconcile()` 返回 `settled` 而不是 `repaired`，recovery 不调用 evaluator / `createId`，不产生 durable write / rename / publish，immutable record、outcome、completed manifest、marker 四份 canonical bytes 稳定；同 operation replay 返回 `already_committed`。该提交只修改测试，没有生产逻辑变化；定向结果为 1 file / 29 tests passed。它不是完整 settlement closure。`ebd084c`（`test(data): cover pre-catalog outcome recovery`）新增独立 `it`，只覆盖已有 `before_catalog_reconcile` 的一次中断：`injectedPoints` 完整有序前缀为 `after_stage_flush` → `after_record_publish` → `after_outcome_publish` → `after_settlement_marker` → `before_catalog_reconcile`；初次 commit 返回 `retryable_failure/reconciliation_required` 且四份 durable 产物已存在；restart `reconcile()` 返回 `settled` 而不是 `repaired`，recovery 不调用 evaluator / `createId`，不产生 durable write / rename / publish，四份 canonical bytes 稳定；第二次 reconcile 仍为 `settled`；同 operation replay 返回 `already_committed`。该提交只修改测试，没有生产逻辑变化；定向结果为 1 file / 30 tests passed。它不是完整 settlement closure。`145b671`（`test(data): cover post-stage-flush outcome recovery`）新增独立 `it`，只覆盖 `after_stage_flush` 中断：stage 保留、canonical record/outcome/marker 缺失、manifest 保持 active；restart reconcile 为 `pending` 且无 durable write；同 operation re-commit 因既有 exclusive stage fail closed，返回 `retryable_failure/reconciliation_required`，不 promote incomplete projections。该提交只修改测试；定向结果为 1 file / 31 tests passed。它不是完整 settlement closure。`d26bb83`（`test(data): cover after-record-publish recovery`）新增独立 `it`，只覆盖 `after_record_publish` 中断：immutable record 已发布而 outcome/marker 缺失、manifest 仍 active；restart `reconcile()` 返回 `repaired`（authority-first，不重跑 evaluator / `createId`），随后 manifest → marker 发布；第二次 reconcile 为 `settled`，同 operation 为 `already_committed`，record/outcome/manifest/marker bytes 稳定。该提交只修改测试；定向结果为 1 file / 32 tests passed。它不是完整 settlement closure。`e743a3e`（`test(data): cover invalid settlement marker residual`）新增独立 `it`，只覆盖 malformed `outcome-settlement.json`：restart `reconcile()` 返回 `review_required` 且 diagnostics 含 `invalid_settlement_marker`；不 rewrite 任何 durable bytes，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 33 tests passed。它不是完整 settlement closure。`a631a31`（`test(data): cover conflicting outcome projection residual`）新增独立 `it`，只覆盖 settled 后 `outcome.json` 与 immutable record authority 分叉：restart `reconcile()` 返回 `review_required` 且 diagnostics 含 `conflicting_outcome`；不 rewrite 任何 durable bytes，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 34 tests passed。它不是完整 settlement closure。`6bfffc5`（`test(data): cover conflicting manifest outcomeRef residual`）新增独立 `it`，只覆盖 settled 后 completed `session.json` `outcomeRef` 与 immutable record authority 分叉（record / outcome.json / marker 保持匹配）：restart `reconcile()` 返回 `review_required` 且 diagnostics 含 `conflicting_outcome`；不 rewrite 任何 durable bytes，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 35 tests passed。它不是完整 settlement closure。`4603601`（`test(data): cover invalid outcome symlink residual`）新增独立 `it`，只覆盖 settled 后 `outcome.json` 被替换为 non-file symlink：restart `reconcile()` 返回 `review_required`；不 rewrite record/manifest/marker，不把 symlink 修复为 regular file，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 36 tests passed。它不是完整 settlement closure。`60b6791`（`test(data): cover invalid settlement marker symlink residual`）新增独立 `it`，只覆盖 settled 后 `outcome-settlement.json` 被替换为 non-file symlink：restart `reconcile()` 返回 `review_required` 且 diagnostics 含 `invalid_settlement_marker`；不 rewrite record/outcome/manifest，不把 symlink 修复为 regular file，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 37 tests passed。它不是完整 settlement closure。`e1f0563`（`test(data): cover invalid session manifest symlink residual`）新增独立 `it`，只覆盖 settled 后 `session.json` 被替换为 non-file symlink：restart `reconcile()` 返回 `review_required`；不 rewrite record/outcome/marker，不把 symlink 修复为 regular file，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 38 tests passed。它不是完整 settlement closure。`f90a863`（`test(data): cover invalid outcome directory residual`）新增独立 `it`，只覆盖 settled 后 `outcome.json` 被替换为 directory（非 symlink）：restart `reconcile()` 返回 `review_required`；不 rewrite record/manifest/marker，不把 directory 修复为 regular file，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 39 tests passed。它不是完整 settlement closure。`5fb4f04`（`test(data): cover invalid settlement marker directory residual`）新增独立 `it`，只覆盖 settled 后 `outcome-settlement.json` 被替换为 directory（非 symlink，可含 junk 内容）：restart `reconcile()` 返回 `review_required` 且 diagnostics 含 `invalid_settlement_marker`；不 rewrite record/outcome/manifest，不把 directory 修复为 regular file，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 40 tests passed。它不是完整 settlement closure。`85840ae`（`test(data): cover invalid session manifest directory residual`）新增独立 `it`，只覆盖 settled 后 `session.json` 被替换为 directory（非 symlink，可含 junk 内容）：restart `reconcile()` 返回 `review_required`；不 rewrite record/outcome/marker，不把 directory 修复为 regular file，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 41 tests passed。它不是完整 settlement closure。`14fa960`（`test(data): cover invalid learning record directory residual`）新增独立 `it`，只覆盖 settled 后 canonical `learning-records/outcome-<sessionId>.md` 被替换为 directory（非 symlink，可含 junk 内容）：restart `reconcile()` 返回 `review_required` 且 diagnostics 含 `missing_record`；不 rewrite outcome/manifest/marker，不把 directory 修复为 regular file，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`。该提交只修改测试；定向结果为 1 file / 42 tests passed。它不是完整 settlement closure。`94e686f`（`test(data): cover invalid learning record symlink residual`）新增独立 `it`，只覆盖 settled 后 canonical `learning-records/outcome-<sessionId>.md` 被替换为 non-file symlink：restart `reconcile()` 返回 `review_required` 且 diagnostics 含 `missing_record`；不 rewrite outcome/manifest/marker，不把 symlink 修复为 regular file，不调用 evaluator / `createId`；同 operation commit 返回 `conflict/review_required`；`f9e263f`（`test(data): cover invalid learning record content residual`）完成严格 **P6-S19 tests-only evidence slice**（invalid canonical learning-record content fail-closed residual）；`412acc5`（`test(data): cover invalid normalized settlement marker residual`）完成严格 **P6-S20 tests-only evidence slice**（well-formed invalid settlement marker fail-closed residual）；`9e47eed`（`test(data): cover invalid learning record metadata residual`）完成严格 **P6-S21 tests-only evidence slice**（well-formed invalid canonical learning-record metadata fail-closed residual）；`a947d4c`（`test(data): cover invalid learning record metadata identity residual`）完成严格 **P6-S22 tests-only evidence slice**（well-formed invalid canonical learning-record metadata identity fail-closed residual）。`a6d693f`（`test(data): cover invalid learning record assessment residual`）完成严格 **P6-S23 tests-only evidence slice**（well-formed invalid canonical learning-record assessment fail-closed residual）。`2fdf59f`（`test(data): cover invalid learning record assessment path residual`）完成严格 **P6-S24 tests-only evidence slice**（well-formed invalid canonical learning-record assessment path fail-closed residual）。`e7440cc`（`test(data): cover invalid learning record body prefix residual`）完成严格 **P6-S25 tests-only evidence slice**（well-formed invalid canonical learning-record body prefix fail-closed residual）；`80788b1`（`test(data): cover empty learning record evidence residual`）完成严格 **P6-S26 tests-only evidence slice**（well-formed empty canonical learning-record evidenceEventIds fail-closed residual）；`fdc2d22`（`test(data): cover invalid learning record evaluatorVersion residual`）完成严格 **P6-S27 tests-only evidence slice**（well-formed invalid canonical learning-record evaluatorVersion fail-closed residual）；`eb2fbf6`（`test(data): cover mismatched learning record sessionId residual`）完成严格 **P6-S28 tests-only evidence slice**（well-formed mismatched canonical learning-record sessionId fail-closed residual）。`74120a7`（`test(data): cover non-canonical learning record operationId residual`）完成严格 **P6-S29 tests-only evidence slice**（well-formed non-canonical canonical learning-record operationId fail-closed residual）。该提交只修改测试；定向结果为 1 file / 53 tests passed。它不是完整 settlement closure。
- **S19 新增 evidence 范围：**`f9e263f` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 invalid canonical learning-record content fail-closed residual：settled 后仅将 canonical `learning-records/outcome-<sessionId>.md` regular-file content 覆写为 invalid non-metadata text；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 record content，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、44 tests passed）。
- **S20 新增 evidence 范围：**`412acc5` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 well-formed invalid settlement marker fail-closed residual：settled 后仅将 `outcome-settlement.json` 覆写为 `{"schemaVersion":2}`（well-formed JSON 但 normalizeMarker 失败）；restart `reconcile()` → `review_required` + `invalid_settlement_marker`；不 rewrite authority、不 evaluate；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、45 tests passed）。
- **S21 新增 evidence 范围：**`9e47eed` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 well-formed invalid canonical learning-record metadata fail-closed residual：settled 后仅将 regular-file record metadata 中 `schemaVersion` 从 `1` 改为 `2`（well-formed metadata 但 readCanonicalRecord schema 校验失败）；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 record metadata，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、46 tests passed）。
- **S22 新增 evidence 范围：**`a947d4c` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 well-formed invalid canonical learning-record metadata identity fail-closed residual：settled 后仅将 regular-file record metadata 中 `recordId` 改为非 canonical 值（schemaVersion 仍为 1，但 identity 校验失败）；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 record metadata，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、47 tests passed）。
- **S23 新增 evidence 范围：**`a6d693f` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 settled 后 well-formed 但 invalid 的 canonical learning-record assessment residual：保持 regular-file record 且 `schemaVersion` 仍为 1、`recordId` 仍为 canonical，仅将 `assessment.contentSha256` 改为非 64-hex；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 record assessment，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、48 tests passed）。
- **S24 新增 evidence 范围：**`2fdf59f` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 settled 后 well-formed 但 invalid 的 canonical learning-record assessment path residual：保持 regular-file record 且 `schemaVersion` 仍为 1、`recordId` 仍为 canonical、`assessment.contentSha256` 仍为 64-hex，仅将 `assessment.relativePath` 改为空串；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 assessment path，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、49 tests passed）。
- **S25 新增 evidence 范围：**`e7440cc` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 settled 后 well-formed 但 invalid 的 canonical learning-record body prefix residual：保持 regular-file record 且 metadata JSON 仍 canonical/well-formed，仅将 markdown 必选 body 前缀 `# Learning outcome: established` 改为不匹配 heading；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 body，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、50 tests passed）。
- **S26 新增 evidence 范围：**`80788b1` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 settled 后 well-formed 但 empty 的 canonical learning-record `evidenceEventIds` residual：metadata 其它字段与 body prefix 仍合法/canonical，仅将 `evidenceEventIds` 置为 `[]`；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 evidence，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、51 tests passed）。
- **S27 新增 evidence 范围：**`fdc2d22` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 settled 后 well-formed 但 invalid 的 canonical learning-record `evaluatorVersion` residual：其它 metadata/body 仍合法/canonical，仅将 `evaluatorVersion` 置为 `null`；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 evaluatorVersion，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、52 tests passed）。
- **S28 新增 evidence 范围：**`eb2fbf6` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 settled 后 well-formed 但 mismatched 的 canonical learning-record `sessionId` residual：path/recordId/body 仍对应当前 session，仅 metadata `sessionId` 与 path session 不一致；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 sessionId，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、53 tests passed）。
- **S29 新增 evidence 范围：**`74120a7` 仅修改 `tests/unit/learning-outcome-committer.unit.test.ts`，补齐 settled 后 well-formed 但 non-canonical 的 canonical learning-record `operationId` residual：其它 metadata/body 仍合法，仅 stored `operationId` 为 upper/mixed case，使 `requireOperationId` 规范化后与 stored 不一致；restart `reconcile()` → `review_required` + `missing_record`；不 rewrite outcome/manifest/marker，不修复 operationId，不调用 evaluator/`createId`；同 operation commit → `conflict/review_required`。无生产语义改动；验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file、54 tests passed）。
- **S2 evidence 的准确边界：**初次 commit 返回 `retryable_failure/reconciliation_required`；record 与 matching outcome 已存在，manifest 仍为 `active` / `outcomeRef: null`，marker 缺失，且未继续 manifest、marker 或 catalog-success。重启后的 reconcile 使用 immutable record authority，返回 `repaired`，不重新运行 evaluator、不重写 outcome，并按 manifest → marker 发布；第二次 reconcile 返回 `settled`，record/outcome/manifest/marker 四份 bytes 稳定；同一 operation 返回 `already_committed`，四份 bytes 仍稳定。验证入口为 `pnpm exec vitest run --project unit tests/unit/learning-outcome-committer.unit.test.ts`（1 file / 28 tests passed），另有 `pnpm run typecheck`、`pnpm run check:security`、`git diff --check` 通过。
- **S3 evidence 的准确边界：**初次 commit 中现有 settlement-marker durable rename 返回 `EIO`；immutable record、`outcome.json` 与已 `completed` 的 manifest 存在而 marker 为 `ENOENT`。重启后的 reconcile 以 immutable record authority 仅发布 marker，evaluator / `createId` 不重跑，record/outcome/manifest 不重写；第二次 reconcile 与同 operation replay 的四份 canonical bytes 稳定。`1334513` 只扩展同一个既有 unit `it`，验证仍为同一 1 file / 28 tests passed（不是新增 test count）；typecheck、security check、diff check 通过。
- **S1/S2/S3/S4/S5/S6/S7/S8/S9/S10/S11/S12/S13/S14/S15/S16/S17/S18/S19/S20/S21/S22/S23/S24/S25/S26/S27/S28/S29 必须分开计数和命名：**S1 是 41 项 unit + 14 项 integration 的历史有限证据；S2 只覆盖单一 `after_outcome_publish` crash window；S3 只覆盖 marker final rename `EIO` failure/restart/reconcile，不是 generic post-manifest；S4 只覆盖单一已有 `after_settlement_marker` interruption 的 settled recovery（29 tests）；S5 只覆盖单一已有 `before_catalog_reconcile` interruption 的 settled recovery（30 tests）；S6 只覆盖 `after_stage_flush` fail-closed recovery（31 tests）；S7 只覆盖 `after_record_publish` authority-first repaired recovery（32 tests）；S8 只覆盖 malformed settlement marker fail-closed residual（33 tests）；S9 只覆盖 conflicting outcome.json vs record authority fail-closed residual（34 tests）；S10 只覆盖 conflicting completed session outcomeRef fail-closed residual（35 tests）；S11 只覆盖 invalid non-file outcome.json symlink fail-closed residual（36 tests）；S12 只覆盖 invalid non-file settlement-marker symlink fail-closed residual（37 tests）；S13 只覆盖 invalid non-file session.json manifest symlink fail-closed residual（38 tests）；S14 只覆盖 invalid outcome.json directory fail-closed residual（39 tests）；S15 只覆盖 invalid settlement-marker directory fail-closed residual（40 tests）；S16 只覆盖 invalid session.json directory fail-closed residual（41 tests）；S17 只覆盖 invalid canonical learning-record directory fail-closed residual（42 tests）；S18 只覆盖 invalid canonical learning-record non-file symlink fail-closed residual（43 tests）。S3 不是泛化 `after_manifest_publish`、完整 manifest failure matrix、生产功能或完整 C-4P6 closure；S4/S5/S6/S7/S8/S9/S10/S11/S12/S13/S14/S15/S16/S17/S18/S19/S20/S21/S22/S23/S24/S25/S26/S27/S28/S29 同样不是完整 C-4P6、完整 catalog/manifest/crash matrix、transaction、rollback、delete、migration、API、operations validation 或 Windows native fsync/power-loss closure。
- **仍未完成：**完整 C-4P6 因 manifest capability-policy、manifest `open` / `write` / `fsync` / `close` 完整矩阵、其它 crash/failure（含尚未单独计为 evidence 的 manifest failpoints 与其它窗口）、跨文件 transaction、rollback、delete、migration、API、operations validation、Windows native fsync/power-loss closure 及完整 close-out 尚未完成，必须继续保留在本待办；S5 仅关闭 `before_catalog_reconcile`；S6 仅关闭 `after_stage_flush` fail-closed 这一条定向 evidence；S7 仅关闭 `after_record_publish` authority-first repaired recovery 这一条定向 evidence；S8 仅关闭 malformed settlement marker fail-closed 这一条定向 evidence；S9 仅关闭 conflicting outcome.json vs record authority fail-closed 这一条定向 evidence；S10 仅关闭 conflicting completed session outcomeRef fail-closed 这一条定向 evidence；S11 仅关闭 invalid non-file outcome.json symlink fail-closed 这一条定向 evidence；S12 仅关闭 invalid non-file settlement-marker symlink fail-closed 这一条定向 evidence；S13 仅关闭 invalid non-file session.json manifest symlink fail-closed 这一条定向 evidence；S14 仅关闭 invalid outcome.json directory fail-closed 这一条定向 evidence；S15 仅关闭 invalid settlement-marker directory fail-closed 这一条定向 evidence；S16 仅关闭 invalid session.json directory fail-closed 这一条定向 evidence；S17 仅关闭 invalid canonical learning-record directory fail-closed 这一条定向 evidence；S18 仅关闭 invalid canonical learning-record non-file symlink fail-closed 这一条定向 evidence；S19 仅关闭 invalid canonical learning-record content fail-closed 这一条定向 evidence；S20 仅关闭 well-formed invalid settlement marker fail-closed 这一条定向 evidence；S21 仅关闭 well-formed invalid canonical learning-record metadata fail-closed 这一条定向 evidence；S22 仅关闭 well-formed invalid canonical learning-record metadata identity fail-closed 这一条定向 evidence；S23 仅关闭 well-formed invalid canonical learning-record assessment fail-closed 这一条定向 evidence；S24 仅关闭 well-formed invalid canonical learning-record assessment path fail-closed 这一条定向 evidence；S25 仅关闭 well-formed invalid canonical learning-record body prefix fail-closed 这一条定向 evidence；S26 仅关闭 well-formed empty canonical learning-record evidenceEventIds fail-closed 这一条定向 evidence；S27 仅关闭 well-formed invalid canonical learning-record evaluatorVersion fail-closed 这一条定向 evidence；S28 仅关闭 well-formed mismatched canonical learning-record sessionId fail-closed 这一条定向 evidence；S29 仅关闭 well-formed non-canonical canonical learning-record operationId fail-closed 这一条定向 evidence。不要删除 design/todo。

## C-5：尚未覆盖的用户动作 correlation 设计门

### P5H：workspace user mutation（mission-first）

- 设计文档：[C-5H Workspace 用户变更 correlation](plans/local-data-workspace-user-mutation-correlation-design.md)
- 当前未获产品 / API 批准，**不可直接实施** `mission_updated` correlation、actionId 或 private receipt。
- 首个候选范围仅为 mission-first；`lesson_style_applied` 的 settings second write 不能被悄然并入。

### P5I：direct-UI lesson generation

- 设计文档：[C-5I Direct-UI lesson generation correlation](plans/local-data-lesson-generation-user-action-correlation-design.md)
- 当前未获产品 / API 批准，**不可直接实施**。需先决定 actionId 生命周期、provider-authority private receipt、receipt retention / authority，以及 `success`、`reused`、`rejected`、`conflict`、`indeterminate` 结果语义。
- 禁止越界：仅覆盖 renderer direct UI 的 `generateLesson` / stream；不覆盖 agent `generate_lesson`、mission、lesson style 或 generic workspace writer。provider outcome unknown 时不得自动重跑。

## C-6：controlled legacy Memory 迁移设计门

- 设计文档：[C-6 受控 legacy Memory 搬迁](plans/local-data-memory-controlled-migration-design.md)
- 已有的 C-6A 只读 aggregate preflight 不授权真实迁移。未来必须先批准可信 main identity / scope、copy → 内部 checksum verify → explicit confirmation → delete、durability / recovery 与审计协议。
- 禁止越界：不得启动、后台或自动迁移，不得由 preflight 暴露或推导 candidate/path/content/hash。
