# ADR-0058 Evidence（ADOPTION 状态措辞与切片状态，历史）

> 本文件保存 ADR-0058 的 ADOPTION 状态措辞快照；正文只保留决策与 residual。不作为开放实现待办。

## ADOPTION 措辞建议（勿直接改 ADOPTION.md 除非主线批准）

- B-02：主进程 façade + drain + 单测 + gateway product invoker 经 `facade.prompt` → **基本完成（ADR-0058）**；residual = autoDrain 关 + mid-run steer IPC + main/renderer 队列同步
- B-01 residual drain 由 B-02 façade 收口；UI banner 仍可标 B-12（[ADR-0067](../0067-cancel-tool-pair-close-and-busy-ack.md)）
